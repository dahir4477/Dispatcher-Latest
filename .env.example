# MongoDB (required for lead capture)
# For Docker Compose, the hostname should match the Mongo service name (default: mongo).
MONGODB_URI=mongodb://dispatch:dispatch_password@mongo:27017/dispatch?authSource=admin
MONGODB_DB=dispatch

# Optional: public site URL (metadata / OG)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
