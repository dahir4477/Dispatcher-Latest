# MongoDB (required for lead capture)
# Docker Compose service name is `mongo`.
MONGODB_URI=mongodb://admin:Koorajaan@mongo:27017/dispatch?authSource=admin
MONGODB_DB=dispatch

# Optional: public site URL (metadata / OG)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
