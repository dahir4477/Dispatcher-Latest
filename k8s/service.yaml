apiVersion: v1
kind: Service
metadata:
  name: dispatch-landing
  labels:
    app: dispatch-landing
spec:
  type: ClusterIP
  selector:
    app: dispatch-landing
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
